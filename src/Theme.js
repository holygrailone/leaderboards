import { createTheme } from "@mui/material";

const AppTheme = createTheme({
  palette: {
    primary: {
      main: "#f59e1d",
    },
    background: {
      main: "#3d3830",
    },
    tonalOffset: 0.5,
  },
});

export default AppTheme;
